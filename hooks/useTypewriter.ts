export const useTypewriter = (text: string, _speed: number = 50): string => {
  // The typewriter effect has been disabled site-wide to prevent any "jumbled" text.
  // This hook now returns the full text immediately.
  return text;
};
